# Docs system
sphinx>=9.0.4
sphinx-rtd-theme>=3.1.0
Pygments>=2.20.0

# Testing dependencies
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-postgresql>=8.1.0
requests-mock>=1.12.1

# Project dependencies
# DATABASE_URL uses the postgresql+psycopg:// dialect (psycopg 3, supports
# both the app's async engine and Alembic's sync one) — previously only
# pulled in transitively via pytest-postgresql, so a production install
# without dev/test deps would have been missing it entirely.
psycopg[binary]>=3.2
websockets>=16.0
sqlalchemy>=2.0.51
fastapi>=0.138.1
pytest-asyncio>=0.26.0
alembic>=1.18.5
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-jose[cryptography]>=3.5.0
bcrypt>=4.0.0
authlib>=1.7.2
httpx>=0.28.1
fastapi-mail>=1.6.5
python-dotenv>=1.2.2
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
apscheduler>=3.11.0