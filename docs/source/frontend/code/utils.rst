Utilities, theme and i18n
=========================

.. todo::

   Reference for the small supporting modules:

   - ``src/utils/`` — ``cardSort``, ``colorTint``, ``dueDateColor``,
     ``labelColor``, ``locale``.
   - ``src/theme/`` — the light and dark MUI theme objects.
   - ``src/i18n/`` — setup and the ``en`` / ``ca`` locale files.
