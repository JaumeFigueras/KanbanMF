Components
==========

.. todo::

   Reference for ``src/components/``. Group them rather than listing them
   alphabetically:

   - Board rendering: ``BoardListColumn``, ``CardItem``, ``CardFace``,
     ``CardDialog``.
   - Dialogs: colour and rename dialogs for boards, lists and cards; label
     picker; checklist; share; archive.
   - Account and settings widgets: avatar upload, language selection,
     notification settings.
   - Routing guards.
