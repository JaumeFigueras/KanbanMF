Cards, checklists and labels
============================

.. todo::

   Planned contents:

   - Creating a card; the card dialog and what each field does.
   - Description, due date, and how the due-date colour changes as it
     approaches.
   - Members and assignees — what the distinction means.
   - Checklists and checklist items; progress display.
   - Extracting a checklist item into a card of its own: what the new card
     inherits from its parent (labels, dates, people, colour) and what it
     doesn't (description, checklists), and that the original item stays put.
   - Labels: creating them per board, colours, and filtering by them.
   - Moving a card within a list, between lists, and between boards.
   - Copying a card; archiving and restoring one.
