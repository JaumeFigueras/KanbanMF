Due-date notifications
======================

.. todo::

   Planned contents:

   - Turning e-mail reminders on for a board — the opt-in is per user and per
     board, so nobody is subscribed on your behalf.
   - Setting the day and hour offsets that decide when a reminder is sent.
   - What a reminder e-mail contains, and why you only ever get one per card
     per offset.
   - Why nothing arrives when the deployment has no SMTP configured.
