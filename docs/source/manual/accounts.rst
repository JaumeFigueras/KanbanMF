Accounts and signing in
=======================

.. todo::

   Planned contents:

   - Signing up with an e-mail address and password; the verification e-mail
     and what happens before it is confirmed.
   - Signing in with Google, and what happens when the same address already has
     a local account.
   - Changing your e-mail address or password, and which of those a Google
     account cannot change here.
   - Display name, initials and avatar upload (including the size limit).
   - Staying signed in across devices, and signing out.
